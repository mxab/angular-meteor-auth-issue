/*
import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  if(!Meteor.users.find().count()){
    console.log('creating test user')
    Accounts.createUser({
      username : "test",
      password : "test"
    })
  }
});
*/
